function raiseToThePowerOf(arr, number) {
  if (arr.length === 0); {
    return null;
  }
for(let i = 0; i < arr.lengh; i++) {
  arr[i] = arr[i]**number;
 }
 return arr;
}


//   }
//   System.out.printIn(Math.pow(arr,number));
// }





//Ignore this line of code
module.exports = raiseToThePowerOf;
